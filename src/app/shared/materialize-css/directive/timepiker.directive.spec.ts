import { TimepikerDirective } from './timepiker.directive';

describe('TimepikerDirective', () => {
  it('should create an instance', () => {
    const directive = new TimepikerDirective();
    expect(directive).toBeTruthy();
  });
});
