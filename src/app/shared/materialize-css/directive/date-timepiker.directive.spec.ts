import { DateTimepikerDirective } from './date-timepiker.directive';

describe('DateTimepikerDirective', () => {
  it('should create an instance', () => {
    const directive = new DateTimepikerDirective();
    expect(directive).toBeTruthy();
  });
});
